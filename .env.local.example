# Database Configuration (Atomic Variables - Primary Method)
# These individual variables are combined to build connection URLs
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=snoball_dev
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=local-dev-password
DATABASE_SSL=false  # Use 'require' for production
DATABASE_POOLING_PORT=6432  # Port for connection pooling

# URLs are built automatically from above variables
# DATABASE_URL will be: postgresql://postgres:local-dev-password@localhost:5432/snoball_dev
# DATABASE_URL_POOLED will be: postgresql://postgres:local-dev-password@localhost:6432/snoball_dev

# Alternative: Full URL Method (overrides atomic variables if DATABASE_HOST is not set)
# DATABASE_URL=postgresql://postgres:local-dev-password@localhost:5432/snoball_dev
# DATABASE_URL_POOLED=postgresql://postgres:local-dev-password@localhost:6432/snoball_dev

# For production (PlanetScale Postgres) - use atomic variables:
# DATABASE_HOST=aws-us-east-1-portal.23.psdb.cloud
# DATABASE_PORT=5432
# DATABASE_NAME=snoball-prod
# DATABASE_USERNAME=your_username
# DATABASE_PASSWORD=your_password
# DATABASE_SSL=require
# DATABASE_POOLING_PORT=6432

# Trading Platform Keys (Alpaca Markets)
ALPACA_API_KEY=
ALPACA_SECRET_KEY=
ALPACA_ENDPOINT=https://paper-api.alpaca.markets  # Use https://api.alpaca.markets for production

# Redis Configuration
# For local development (Docker)
REDIS_URL=redis://default:local-dev-password@localhost:6379

# For production (Render Redis - automatically provided)
# Render will automatically set this when you add a Redis service
# REDIS_URL=redis://red-xxxxx:6379

# Alternative: Upstash Redis (if you prefer external service)
# REDIS_URL=rediss://default:password@upstash-redis-endpoint:6380

# AWS Configuration (optional - only if using S3 or other AWS services)
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# Application Environment
NODE_ENV=development
ENVIRONMENT=development

# Next.js Configuration
NEXT_PUBLIC_API_URL=http://localhost:9090

# Authentication (WorkOS)
WORKOS_API_KEY=
WORKOS_CLIENT_ID=
WORKOS_WEBHOOK_SECRET=
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Security
ENCRYPTION_KEY=  # 32-character random string for API key encryption

# LLM Configuration
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
